# secretos
.env
.env.*
!.env.example
pass.txt
*.pem
*.key

# fuentes y derivados pesados (fuera del repo; catálogo sí)
FUENTES/*.pdf
FUENTES/**/*.pdf
FUENTES/ocr/
index/*.db
index/*.db-*
data/
backups/

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
eval/reports/

# web
web/node_modules/
web/dist/
web/.astro/

# so
.DS_Store
Thumbs.db
~$*

# derivados de la ingesta (se regeneran con make ingest)
index/chunks/
index/ingest_run.log

# astro
web/site/node_modules/
web/site/dist/
web/site/.astro/
eval/flagged.jsonl

# La clave de servicio de Search Console. Nunca al repositorio.
gsc_key.json

# La sal de los hashes de IP: propia de cada despliegue, nunca al repositorio.
.ops_salt

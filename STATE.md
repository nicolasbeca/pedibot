# STATE.md — estado vivo de PediBot v2

Última actualización: **2026-09-08** — **EN PRODUCCIÓN en https://pedibot.xyz**.

> Nota de la revisión del 8-sep: la cabecera de este fichero decía «25-ago» mientras el código
> iba por el 7-sep, con 234 commits y catorce días de trabajo sin recoger. Un estado vivo que
> se queda quieto miente con más eficacia que no tener ninguno.

## Fase actual

**F1–F3 HECHAS. F4 Despliegue — HECHO el 25-ago**: VPS Hetzner `46.225.74.13` (1 vCPU / 2 GB, Ubuntu 24.04, hostname `pedibot`), Caddy con certificado Let's Encrypt para `pedibot.xyz` (DNS en Cloudflare, nube gris), units `pedibot-api`, `pedibot-telegram` (@Pedichat_bot), watchdog cada 10 min (alertas a @Pedibot_ops_bot), backup 05:30, guías lunes/jueves 07:00. Despliegue desde el PC: `bash ops/deploy.sh 46.225.74.13` (tar sobre SSH; la web se construye en el PC con `make web-build`). **F5 beta cerrada EN MARCHA desde el 26-ago** (primer probador externo: un amigo del operador; objetivo 10-20 padres, revisar TODAS las conversaciones en `/admin` y marcar las malas con el botón de flag).

Para probarlo en el navegador: `uv run pedibot serve` y abrir http://127.0.0.1:8601 (con `LLM_PROVIDER=fake` en `.env` funciona sin clave, pero las respuestas serán el fallback "no tengo fuente"; con `DEEPSEEK_API_KEY` responde de verdad).

## Decisiones del operador (24-ago, respuestas a las dudas D-01…D-13)

| Duda | Decisión |
|---|---|
| D-01 dominio | **pedibot.xyz** — comprado el 25-ago, DNS en Cloudflare (en registro). |
| D-02 repo | Solo local por ahora; a GitHub más adelante. |
| D-03 licencias | Propuesta aceptada: obras de referencia = `citar_solo`; Elsevier excluido. |
| D-04 mercado | **Internacional desde el inicio. Inglés primero, español después, otros idiomas más tarde.** |
| D-05 año hojas SEUP | Buscarlo nosotros. En seup.org la serie cuelga de `wp-content/uploads/2025/05/` (subida mayo 2025); el año de edición no consta en el texto → citamos sin año hasta confirmarlo. |
| D-06 X | Creía que había API gratis; **no la hay desde feb-2026** (pago por uso, 0,20 $/post con enlace) → **X descartado**; el generador deja el texto listo para pegar a mano. |
| D-07 artículos | **Auto-publicar** (con verificador de citas como único filtro). |
| D-08 SEO | Sin datos previos; se parte de cero con Search Console. |
| D-09 token | La política del PRD §9 es punto de partida. El operador tiene muchos PDBT. |
| D-10 legal | Sin asesoría; disclaimer genérico. |
| D-11 donaciones | No por ahora; el token es la vía de financiación. |
| D-12 VPS | Más adelante; ahora todo local. |
| D-13 LLM | DeepSeek (el más barato). |

## Qué hay construido (commit de esta sesión)

| Bloque | Estado | Dónde |
|---|---|---|
| Ingesta | ✅ PDF (pymupdf) **y HTML** (BeautifulSoup: `main`/`#topic-summary`, sin nav/pies/formularios; los `h2/h3` hacen de títulos) → limpieza → secciones (tamaño de fuente/mayúsculas/"¿…?") → chunks (320-480 palabras, fusión de trozos < 40) → clasificación por reglas (`config/taxonomia.yaml`) → JSONL + SQLite FTS5. **210 documentos, ~5.500 chunks, 278 con posología** (antes de las fuentes web: 47 docs / 4.792 chunks). Idempotente por hash. Informe en `index/ingest_report.csv`. | `src/pedibot/ingest/`, `src/pedibot/index/` |
| Catálogo | ✅ **212 fuentes**: 49 PDF (`config/fuentes.yaml`) + **163 páginas web públicas internacionales** descargadas el 25-ago (`config/fuentes_web.yaml`, generado por `scripts/fetch_web_sources.py`): NHS 57 (OGL v3), MedlinePlus 50 EN + 24 ES (dominio público, solo el resumen), CDC 19 (dominio público), OMS 8 EN + 6 ES (CC BY-NC-SA). Descartadas por licencia: Raising Children, HealthyChildren/AAP, KidsHealth. Detalle y licencias en `FUENTES/CATALOGO.md`. | `config/fuentes*.yaml` · `FUENTES/CATALOGO.md` |
| Triaje | ✅ 31 reglas es/en (`config/red_flags.yaml`), 3 niveles + salud mental, parser de edad, regla dura "<3 meses + fiebre". 34 casos de test (recall y precisión). | `src/pedibot/bot/triage.py` |
| Calculadora de dosis | ✅ paracetamol / ibuprofeno con rangos de la guía AEPap, topes duros, edad/peso mínimos, ml por presentación; test por fila + property-based (hypothesis) de que nunca supera los topes. | `src/pedibot/bot/dose.py` |
| Motor de respuesta | ✅ pipeline completo: triaje → (enrutador de dosis determinista) → (pregunta la edad si falta con fiebre) → recuperación con expansión cross-lingüe (`config/synonyms.yaml` + LLM opcional) → prompt `answer_v1` → **verificador** (citas existentes, ninguna cifra mg/ml sin tabla de dosis) → 1 regeneración → fallback "no tengo fuente" → ensamblado con banner por país (`config/emergency_numbers.yaml`, 18 países). Proveedor LLM intercambiable (`FakeProvider` en tests; DeepSeek vía cliente OpenAI). | `src/pedibot/bot/answer.py`, `retrieval.py`, `llm.py`, `prompts/answer_v1.md` |
| API HTTP | ✅ FastAPI: `POST /api/ask` (JSON: question, country, lang, session), `POST /api/feedback` (👍/👎 solo desde la sesión propietaria), `GET /api/health`, `GET /api/stats`. Registro anonimizado en `data/pedibot_ops.db` (sesión aleatoria, IP solo como hash con sal, tokens/coste/latencia/versión de prompt). Rate limit por IP (20/10 min, 200/día) y **modo degradado** al superar `MAX_DAILY_LLM_USD`: devuelve pasajes sin LLM. CORS solo a `ALLOWED_ORIGINS`. 7 tests con `TestClient`. `pedibot serve` arranca uvicorn en 127.0.0.1:8601. | `src/pedibot/api.py`, `src/pedibot/ops/store.py` |
| Publicación | ✅ Generador de artículos (`publish/articles.py`): plan de **73 temas** (28 SEUP + 45 anclados en NHS/MedlinePlus/CDC/OMS, 25-ago) anclados en hojas para padres, prompt `article_v1` (TITLE/SUMMARY/BODY con 4 secciones fijas, siempre "cuándo ir a urgencias"), **mismo verificador de citas/dosis que el bot** (1 reintento, si falla se descarta), salida Markdown con frontmatter (`web/content/<lang>/<slug>.md`) + texto para X en `publish/queue/x/`. **Sindicación automática** (`publish/social.py`, 25-ago): Bluesky y canal de Telegram (gratis) si hay credenciales; X solo con `X_POST_ENABLED=true` (pago por uso, ~0,20 $/post con enlace). **Cadencia en producción: diaria 07:00 UTC, 1 EN + 1 ES.** RSS: `/rss.xml` y `/es/rss.xml`. `pedibot publish [--topic] [--lang] [--n] [--fake]`. 5 tests. Sin LLM real todavía. | `src/pedibot/publish/` |
| Web (prototipo servible) | ✅ `web/static/index.html`: el boceto convertido en página real — el chat llama a `/api/ask`, renderiza banner/citas/fuentes con enlace al PDF, 👍/👎 a `/api/feedback`, selector de país (auto por idioma del navegador), sesión en localStorage. La API la sirve en `/` (`pedibot serve` → http://127.0.0.1:8601). Probado end-to-end con `LLM_PROVIDER=fake`. Astro/i18n/artículos siguen pendientes (Node). | `web/static/` |
| Memoria de conversación (I-31) | ✅ `Engine.ask(..., history=[...])`: ventana de 6 turnos; la edad dicha en un turno anterior cuenta (regla <3 meses incluida); los síntomas viejos NO re-disparan el banner; la recuperación de un seguimiento corto ("¿y si además vomita?") usa también el mensaje anterior; el prompt recibe CONVERSATION SO FAR. Turnos por sesión en `data/pedibot_ops.db` (24 h). Probado con el modelo real: el seguimiento recupera la hoja de vómitos y mantiene los 4 años. | `bot/answer.py`, `ops/store.py`, `api.py` |
| Kit de despliegue (F4) | ✅ `ops/`: `bootstrap.sh` (paquetes, Caddy, Node, usuario `pedibot`, uv, ufw), `deploy.sh` (rsync código + índice + dist, `uv sync`, units, Caddy reload), `Caddyfile` (pedibot.xyz, cabeceras, `/api/*` y `/a/*` → 8601), units systemd (`pedibot-api`, watchdog 10 min con **aviso de saldo DeepSeek <20 % por Telegram**, backup 05:30, publish lunes/jueves 07:00). Paso a paso en `ops/README.md`. Sin probar hasta tener el VPS. | `ops/` |
| Telegram (chatbot público) | ✅ `src/pedibot/telegram_bot.py`: el mismo `Engine`, sesión = hash del chat (sin datos personales), memoria por chat, botones 👍/👎, `/country`, `/lang`, `/help`. `pedibot telegram` (long polling) + `ops/systemd/pedibot-telegram.service`. Necesita `TELEGRAM_PUBLIC_BOT_TOKEN` (bot NUEVO de @BotFather; el de alertas es otro: `TELEGRAM_BOT_TOKEN`). 3 tests sin red. | `telegram_bot.py` |
| Suero oral (I-30) | ✅ `bot/ors.py`: pauta por edad tomada del **prospecto AEMPS de Sueroral Hiposódico** (CIMA 59877: lactante >1 mes 1–1,5× la toma; ≥1 año ~200 ml por deposición en tandas de 25–30 ml cada 10–15 min) y de la hoja SEUP de vómitos (5–10 ml cada 10 min). `/api/ors`, sección en `/dose`. El prospecto está en el índice. El PIL de Dioralyte (emc) se descartó: la página se renderiza con JS y no capturó texto. | `bot/ors.py` |
| Herramientas deterministas nuevas (26-ago) | ✅ **Vacunas** ES/GB/US (`config/vaccines.yaml` transcrito del Ministerio 2025, NHS y CDC 2025; `/vaccines`, `/api/vaccines`; el chat enruta preguntas de vacunas con país conocido). **Foto** de signos de alarma (`bot/photo.py`, `deepseek-v4-flash-vision-exp`, ≈0,0001 $/foto; solo petequias/cianosis/hinchazón; probado en producción). **Aclaración** (mensaje vago que menciona al niño → 7 opciones). **Avisos** de salud pública (`ops/alerts_fetch.py` → banner en la home). **Panel `/admin`** con basic auth (métricas 7/30/90 d, PDBT, conversaciones, flag). **Guías comparativas** y **calendario estacional** en publish. **`/api/agent/ask`** para ACP (`AGENT_API_KEYS`). | ver IDEAS «Lote del 26-ago» |
| Canales (26-ago) | ✅ **Bluesky `@pedibot.bsky.social` conectado y publicando** (primera publicación real verificada: guía de golpe de calor EN+ES). **Sin canal de Telegram** (decisión operador): en su lugar, avisos MUY puntuales a quien haya usado el bot — `pedibot announce "…"` con guarda de 14 días, línea de baja y `/stop`; la audiencia son chat ids en `tg_users` (documentado en /legal). **X (@pedibotai) recuperado y en marcha el 26-ago** — publicación manual desde `publish/queue/x/`. Bloque «Dónde está PediBot» en la home + enlaces en pie y compositor. Verificación de Google Search Console servida en `/google73213559d998fa6d.html`. | `publish/social.py`, `announce.py` |
| **Caché del HTML (2-sep)** | ✅ El HTML se servía **sin ninguna cabecera de caché**, así que el navegador se quedaba con la copia vieja a su criterio: un cambio desplegado era invisible para quien ya había visitado la web (así se detectó — la pestaña de apoyo estaba servida y el operador no la veía). Ahora todo revalida (`no-cache`, que con el ETag cuesta un 304 de unos bytes) salvo `/_astro/*`, que lleva hash en el nombre y se cachea un año. Ojo con el matcher: por extensión se dejaba fuera `/support` y todas las URL limpias, así que la regla está invertida. |
| **Página de apoyo reescrita (1-sep)** | ✅ Estaba solo en el pie: ahora hay **pestaña ♥ en la cabecera**, y a propósito en el grupo derecho y no en el `<nav>`, porque ese se oculta en móvil y esta es justo la página que pide ayuda. Contenido nuevo en un solo componente para los dos idiomas (antes eran dos ficheros que iban a divergir): apertura honesta, la cita del operador de por qué existe, **lo que hace sin pedir nada** (214 documentos, cada frase con su organismo, dosis de tablas fijas, 30 guías, cero cuentas y cero rastreadores) y **cuatro formas de ayudar, con el dinero en último lugar** — contárselo a otro padre, marcar una respuesta mala, revisar como pediatra (mailto con asunto) y donar. Las cuatro tarjetas llevan a algún sitio: una petición sin forma de actuar es un callejón sin salida. El token pasa a dos líneas al final. Sin cifras de coste en ningún punto. |
| **Donaciones (1-sep)** | ✅ Bloque en `/support` y `/es/support`: dirección, QR generado desde la misma constante que muestra el texto, botón de copiar y enlace EIP-681 que abre la cartera. **Sin librería de wallet ni servicio de QR externo** — `/legal` promete que no hay peticiones a terceros. Dirección `0x1EC1…eaBb`, elegida por el operador **porque ya era pública a través de Virtuals**: no expone nada nuevo, pero sí deja a la vista su saldo y su historial (1.214 transacciones en Base). Comprobada con `eth_getCode` en las seis redes que se listan (Base, Ethereum, Arbitrum, Optimism, Polygon, BNB Chain): cuenta normal en todas, recibe sin problema. Avisos: elegir una red EVM de la lista y que un envío desde una red no EVM (USDC en Solana o Tron) se pierde. Tests que bloquean el contrato de PDBT y la cartera del agente ACP como destino. |
| **Catálogo ACP completo (1-sep)** | ✅ Aplicado lo aprendido con el agente Regime. **4 trabajos a 0,01 USDC** (el suelo del mercado; decisión del operador: visibilidad, no rendimiento): `paediatric_question_with_sources` (renombrado de «Pediatric answer with sources» — el mercado usa snake_case), `child_medicine_dose`, `childhood_vaccination_schedule`, `oral_rehydration_plan`. **5 recursos gratis**: catálogo de las 214 fuentes (`/sources.json`, publicado hoy), tabla de marcas por país, checklist de urgencias, calendarios de vacunas y el feed de guías. **2 paquetes**: `pedibot_answers_7d` 0,25 y `pedibot_answers_30d` 0,50, enganchados a los cuatro trabajos. Un trabajo genérico no aparecía en ninguna búsqueda; ahora cada producto es su propia ficha. |
| **Esquemas ACP completos (1-sep)** | ✅ El mercado marca en amarillo el esquema al que le falta descripción — **la del objeto y la de CADA propiedad** —, y es justo lo que lee un agente comprador para decidir si sabe usar el trabajo. Estaban incompletos los cuatro (17 campos sin describir en total, incluida la oferta original del 26-ago). Rellenados y verificados con `ops/acp_schemas.sh`, que es re-ejecutable. Regime se comprobó también: estaba completo. ⚠️ **Queda un aviso**: el parámetro `lang` del recurso `emergency_department_checklist` no tiene descripción y en esta versión de la CLI `acp resource update` y `delete` **no aceptan opciones** (son stubs), así que solo se arregla desde el panel web. |
| **Trabajador ACP: precio y enrutado (1-sep)** | ✅ **Bug**: proponía el presupuesto desde `ACP_PRICE_USDC=0,05` mientras la ficha decía 0,01 — cinco veces lo anunciado al primer comprador. Ahora lee `acp offering list` al arrancar, lo relee cada 30 min y, si no identifica la oferta, cobra la más barata. **Enrutado nuevo**: cada formulario va a su endpoint (`/api/dose`, `/api/vaccines`, `/api/ors`, `/api/agent/ask`), así que las dosis y los calendarios salen de tablas fijas y **el modelo no interviene en ninguna cifra**. Probados los cuatro end-to-end contra la API viva antes de publicarlos; el esquema del suero se corrigió porque no casaba con lo que devuelve de verdad. 10 tests. |
| Virtuals ACP (26-ago) | ✅ **Agente PediBot creado en EconomyOS** (`01a03e84…7354`), **token PDBT vinculado** (no se lanzó ninguno nuevo), wallet Privy EVM `0x30d3…3ebb` + Solana, correo `pedibot@agents.world`, **firmante añadido por CLI** (clave P256 en el llavero del servidor, política `restricted`), **oferta publicada** «Pediatric answer with sources» (0,05 USDC, SLA 5 min, esquemas JSON de entrada/salida). Servicio `pedibot-acp` corriendo: sondea `acp job list` cada 30 s, propone presupuesto y entrega la respuesta del motor vía `/api/agent/ask`. Sondea con `acp job list` a secas — con `--all` la política `restricted` lo colgaba 3 min por ciclo (L18, arreglado y verificado: 0 timeouts). **Sin trabajos todavía** — la forma exacta de los payloads se registrará con el primero. ⚠️ `restricted` sigue pidiendo aprobación manual en algunas llamadas RPC. Todo lo aprendido: `ops/VIRTUALS_APRENDIDO.md` (copia en `D:/Nicolas/Regime_Virtuals/`). | `ops/acp_worker.py` |
| CLI | ✅ `pedibot ingest / search / triage / dose / ask [--fake] / eval [--llm --judge] / serve / publish / balance` | `src/pedibot/cli.py` |
| Golden set + eval | ✅ `eval/golden.jsonl` (60 preguntas es/en con nivel, reglas, documento esperado o ruta esperada) y `pedibot eval` (sin LLM). **Resultado 26-ago: 60/60 en las seis métricas** (triaje, recall y precisión de red flags, reglas, fuente en top-3 y enrutado). Los 2 fallos que quedaban eran cruzados de idioma y se cerraron el 26-ago (L20). ⚠️ Con 60 casos y términos añadidos después de ver los fallos, un 1,0 significa "sin fallos conocidos", no "recuperación perfecta": el golden set está saturado y hace falta ampliarlo para que vuelva a medir. Informe en `eval/reports/`. Mejoras que lo lograron: pesos por tipo de documento (hoja_padres ×1,6, libro ×0,55), boost por tema de la taxonomía, sinónimos es→es coloquiales, filtro de bibliografías en la ingesta, regla fuera-de-ámbito (sin tema pediátrico → 3 términos o silencio). | `src/pedibot/eval.py` |
| Tests | ✅ **171 tests verdes**, ruff + mypy limpios. | `tests/` |
| Web Astro (F3) | ✅ **Node 24 instalado el 25-ago (winget, autorizado por el operador)**. Sitio en `web/site` (Astro 7 + sitemap): i18n `en` (raíz) / `es` (`/es`), layout con canonical/hreflang/OpenGraph/JSON-LD (Organization, WebApplication, MedicalWebPage), tema **siempre claro** (noche solo con el botón; el ajuste del navegador se ignora — decisión operador), tokens del boceto v2. Páginas: home chat-first (widget real contra `/api/ask` con chips de país/edad/peso enviados en el mensaje, sesión y país en localStorage, 👍/👎, `?q=` precarga), `/dose` (calculadora contra `/api/dose` con marcas), `/sources` (tabla desde `config/fuentes.yaml` vía `scripts/export_catalog.py`), `/guides` + `/guides/<slug>` (colección desde `web/content/<lang>/*.md`). **Primeras 2 guías reales generadas con DeepSeek** (fiebre EN+ES, 0,0006 $ cada una). `make web-build` → `web/site/dist`, que la API sirve en `/` cuando existe (probado: 10 páginas, chat real OK). | `web/site/`, `web/content/` |
| Boceto | 🎨 v1 y v2 (`web/mockups/`), v2 = dirección aprobada. | artefactos publicados |

## LLM real (desde el 25-ago)

- Clave en `.env` (el operador la dejó como `.env.txt`; renombrada). Saldo cargado: **10 USD el 25-ago**. `uv run pedibot balance --initial-usd 10` consulta el saldo real y avisa por debajo del 20 % (exit 2) — **pendiente cablearlo al watchdog/Telegram en F4**; mientras, se comprueba a mano en cada sesión.
- Modelo verificado: `deepseek-v4-flash` (también existen `deepseek-v4-pro` y `-vision-exp`). **V4 razona por defecto**: los tokens de razonamiento se facturan como salida y consumen `max_tokens` (la primera respuesta se cortó a 900 tokens). Desactivado con `extra_body={"thinking": {"type": "disabled"}}`. Coste medido: **0,0003-0,0005 $ por respuesta** (≈ 2.000-3.000 tokens de entrada, 180-240 de salida), 8-12 s de latencia.
- Dos correcciones que solo se vieron con el modelo real: (1) respondía en el idioma de las fuentes (español) a preguntas en inglés → línea `ANSWER LANGUAGE` explícita; (2) a un bebé de 2 meses con fiebre le sugería "paracetamol o ibuprofeno a la dosis de su pediatra" copiando la hoja genérica → contexto de edad en el prompt (`<3 meses: nada de medicación en casa`, `<6 meses: sin ibuprofeno`). Con eso la respuesta pasa a "no le dé ningún medicamento sin indicación médica; debe ser evaluado hoy".
- El AVG **no** mató la conexión TLS en este Windows (L06 no aplicó aquí).
- **`pedibot eval --llm` (25-ago, 55 preguntas del golden set con `deepseek-v4-flash`)**: 54 redactadas (1 fuera de ámbito → silencio correcto), **validez de citas 1,0** (ninguna cita inventada, ninguna cifra de dosis fuera de tabla), **0 regeneraciones**, 100 % con fuentes, coste total **0,0198 $** (0,00037 $/respuesta), latencia p95 3,9 s. Informe: `eval/reports/eval_llm_2026-08-25.json`. Saldo tras la prueba: 9,98 $. - **Juez de fidelidad (`eval --llm --judge`, 25-ago, 1.ª pasada, prompt v2)**: 40/54 "fiel" (**74 %**), 6 "minor_issue", 8 "unfaithful". Diagnóstico: 6 de las 8 eran frases de seguridad que el modelo copió de nuestro contexto de edad/banner sin pasaje que citar (L16); 2 eran contaminación del boost de signos de alarma entre temas (umbrales de la hoja de golpe de calor atribuidos a la de fiebre, L17); 1 (g20) inventó el momento de la triple vírica. Correcciones aplicadas (inyección del chunk de la regla como pasaje citable + boost por tema). **2.ª pasada: 78 %** (42/54), validez de citas 1,0, 0,036 $ la pasada. Restos: g58 (golpe de calor sigue colándose en fiebre → penalización fuera de tema añadida), g13 (diarrea recuperaba pasajes clínicos, no la hoja SEUP → sinónimo diarrea→gastroenteritis), g20 (inventa fechas de vacunas → argumento para la herramienta tabulada I-19), y varios "infieles" discutibles del juez ("llama a emergencias" vs "acude a urgencias"). El juez es una **señal ruidosa**: sirve para encontrar clases de fallo, no como cifra de calidad; la lectura humana de respuestas sigue siendo el gate de la beta (F5). Coste con juez: 0,00066 $/respuesta.
- **Prompt `answer_v2`** (25-ago): la fuente se nombra en la frase ("According to the SEUP…") además del [n]; tono calmado; regla explícita sobre instrucciones de seguridad sin pasaje.
- **Calculadora con marcas** (`config/drugs.yaml`): 20 marcas de 12 países para paracetamol/ibuprofeno con su concentración; `/api/dose`, `/api/drugs`; el chat enruta "how much Calpol for 14 kg" a la calculadora.

## Políticas de comunicación (decisiones del operador)

- **Costes e interioridades son privados** (25-ago): la web y los textos públicos dicen que mantener PediBot cuesta dinero y que el token lo financia, pero **no cifras** (servidor, coste por respuesta, saldo) ni detalles de infraestructura. El «libro de cuentas» público se retiró; en su lugar, tarjeta «qué financia el token» y novedades periódicas de producto. Los informes con cifras van solo al Telegram del operador.
- **Respuestas sin bloque de fuentes** (25-ago): el organismo se nombra en la frase; sin enlaces, títulos ni marcadores. Máximo ~110 palabras.

| **Aviso de compra/venta de $PDBT (27-ago)** | ✅ `ops/token_alert.py` + `pedibot-token-alert.timer` **cada hora**. Lee las operaciones reales de la pool (PDBT/VIRTUAL en `virtuals-unicorn-base`, donde **PDBT es el token base**, así que el `kind` de la API ya es desde nuestro punto de vista) y avisa solo de las que no ha visto, identificadas por hash de transacción. **Sin novedades no manda nada.** Primera ejecución = línea base, para no anunciar de golpe lo viejo; ráfagas de más de 5 se resumen en un mensaje. Límite conocido: el endpoint solo cubre 24 h — si el timer se para un día entero, la operación sigue contándose en el resumen diario y sale en el informe del domingo, pero sin push. 9 tests. |

### Fase francesa — arrancada el 2-sep-2026 (orden del operador: fuentes → triaje y terminal → guías y web)

| Paso | Estado |
|---|---|
| **Calendarios FR y DE** | ✅ Transcritos de las fuentes primarias y en producción (`/vaccines/fr`, `/vaccines/de`, ambos idiomas + API + ficha ACP actualizada). Francia: calendrier 2026 — MenB (3/5/12 m) y MenACWY (6+12 m) **obligatorias desde el 1-ene-2025**; contrastado en dos páginas oficiales porque la primera lectura daba MenB a los 2 meses (era un error de extracción). Alemania: **STIKO 2026 del RKI (edición oficial en inglés)** — MenB en lactantes 2/4/12 m y **MenACWY nueva a los 12-14 AÑOS, desaparece MenC a los 12 meses**: transcribir del PDF vigente y no de memoria evitó publicar el calendario del año pasado. Tests-candado con los hechos de 2026. |
| **Fuentes francesas (paso 1)** | 🔶 Rastreo de licencias hecho y documentado en `FUENTES/FRANCES.md`: **SpF y ameli cerradas** (derechos reservados, permiso escrito), **OMS-FR y Canada.ca abiertas** (clase NC, como la OMS ya aceptada), **sante.gouv.fr sin verificar** (WAF con CAPTCHA). **Decisión bloqueante planteada al operador**: corpus abierto, pedir permiso, o ambas (recomendada). |
| **Fuentes francesas (paso 1)** | ✅ **Opción A del operador**: corpus abierto, sin SpF ni ameli. 17 documentos nuevos — 7 espejos de la OMS en francés (misma licencia ya aceptada) y 10 páginas de Canada.ca cuyas URL francesas se resolvieron **desde el conmutador de idioma de cada página y se verificaron con un 200**, no adivinando el slug. Índice: 5.632 pasajes, 228 documentos, 132 en francés. |
| **Triaje y terminal (paso 2)** | ✅ **La capa de seguridad lee francés**: las 25 reglas con `reason_fr` y patrones franceses, edades («2 mois», «3 ans», «6 semaines», «10 jours de vie»), detector de idioma a tres bandas, banner y textos, y la API/Telegram abiertos a `fr` (antes el motor forzaba a inglés cualquier idioma que no fuera es/en). **10 casos franceses en el golden set: 70/70 en las cuatro métricas.** Puente de sinónimos fr→fr y fr→en, con el impulso a «idiomas escasos» **medido del propio índice** (<10% del corpus) para que 17 documentos franceses no queden enterrados bajo 228 — impulso al idioma escaso, nunca castigo a los demás, que fue lo que falló en agosto. Trampa de homógrafos repetida y resuelta: «cogné la tête» no es «mal de tête». |
| **Guías y web en francés (paso 3)** | ✅ **PediBot habla francés de punta a punta.** Web completa en `/fr` (12 páginas: portada con chat, guías, dosis, vacunas, urgencias, diario, fuentes, apoyo, legal, RSS) con las 39 cadenas de interfaz traducidas, y **60 guías francesas** generadas sin un solo rechazo. Sitemap: 283 URLs. Selector de idioma y `hreflang` reescritos para **tres** idiomas — eran binarios («el que no soy») y ahora cada guía declara sus dos gemelas o ninguna si no existe. |
| **Lo que estaba cableado a dos idiomas** | Aparecieron seis sitios donde «el otro idioma» estaba escrito a mano y que habrían roto el francés en silencio: el motor forzaba a inglés todo lo que no fuera es/en; la API y Telegram rechazaban `fr`; el generador de artículos pedía **«Spanish»** para el francés (por eso la primera tanda salió entera en español); el esquema de contenidos de Astro solo admitía `en`/`es` y tumbaba **toda** la construcción por una guía; el descargo y el encabezado de fuentes eran ternarios; y el detector de duplicados saltaba el francés sin decirlo. Todos centralizados ahora (`SUPPORTED_LANGS`, `LANGUAGE_NAME`, `LANGS`). |
| **Lo que se veía feo (3-sep)** | La página de **urgencias en francés salía vacía**: 34 signos y 3 encabezados en blanco, porque `checklist.json` solo tenía `en`/`es` y el componente indexa por idioma sin respaldo — una traducción que falta no es un error, es una cadena vacía. Con ella, seis páginas `/fr` conservaban su texto en castellano (título, aviso legal, fuentes, dosis, vacunas, diario) y el widget de vacunas hablaba inglés. Todo venía de la misma raíz que ya se había arreglado en el motor: **el idioma decidido con un ternario `lang === 'es' ? … : …`**, quince veces en los componentes de la web y doce más en Python. Ahora hay un catálogo por idioma (`i18n.ts`, `bot/strings.py`) y **cuatro tests candado**: mismas claves en los cuatro idiomas, mismos idiomas en los datos, ningún ternario binario en `.astro`, y un rastreo del sitio construido que falla si un titular lleva palabras de otro idioma. |
| **Menú de idiomas (3-sep)** | Con dos banderas la cabecera ya desbordaba entre 720 y 950 px y en el móvil (marca + ♥ + ES + FR + 🌙 no caben en 360 px); con la tercera se hizo evidente y con la cuarta habría sido peor. Un solo control `<details>` cuyo ancho no depende de cuántos idiomas haya, que abre sin JavaScript y responde al teclado, más un `nav` que suelta sus enlaces menos importantes al estrecharse (1020 → 900 → 800 px) en vez de empujar la página de lado. |
| **Guías con idiomas mezclados (3-sep)** | 143 encabezados corregidos en 181 guías: `article_compare_v1.md` listaba sus cuatro secciones **solo en inglés**, así que las guías comparativas salían con títulos ingleses en español y francés; 26 guías francesas llevaban «Preguntas frecuentes» en castellano; y las guías españolas más antiguas inventaban nueve formas distintas de decir «cuándo acudir al médico». La de la FAQ además costaba SEO: `guides.ts` solo reconocía el encabezado inglés y español, así que **ninguna guía francesa publicaba datos estructurados de preguntas**. Y el «tema» que se mostraba como etiqueta era el identificador interno (`vacunas`, `head_injury_en`), visible en las tres webs. |
| **Un cuarto sitio con el mismo bug** | El prompt de las respuestas nombraba el idioma con `{"en": "English", "es": "Spanish", "fr": "French"}.get(lang, "English")`: una pregunta en alemán se contestaba **en inglés** (comprobado en vivo antes de arreglarlo). Es la misma tabla que en julio hizo que las guías francesas se pidieran en español. Ahora existe una sola, en `bot/strings.py`, usada por el motor y por el generador, con un test que falla si alguien la vuelve a escribir a mano. |
| **El token, a tres redes (3-sep)** | Decisión del operador: PDBT sale también en **Solana (Jupiter Studio)** y **HyperEVM (LiquidLaunch)**, además de Base/Virtuals. El token es el mecanismo de financiación desde el principio y **cada red se lanza desde una cartera distinta** — ninguna es la de donaciones de la web. La web ya lo dice en los cuatro idiomas: los despliegues viven en `web/site/src/tokens.ts`, una entrada por red, y las dos pendientes se muestran como «anunciado, todavía sin desplegar» **sin enlace a ningún contrato** (un enlace a un contrato que no existe es la forma exacta que tendría una copia falsa). Añadido el aviso de que solo son nuestras las direcciones de esa página. Cuando existan los contratos, activarlos es una línea por red. |
| **⚠️ Repaso general 3-sep: números de emergencia extranjeros en 43 guías** | Una auditoría de ocho frentes en paralelo, con cada hallazgo pasado por tres refutadores, encontró lo peor que había en el sitio: **guías que mandaban a un padre a marcar un número que no existe en su país**. La guía española de meningitis decía «Llame al 999»; la rusa de fiebre, «Вызывайте скорую (999)»; la árabe daba 999 y 111 para signos de meningitis. Son números del Reino Unido, muertos en España, Rusia o Egipto, y estaban en el párrafo que un padre asustado lee primero. Origen: la regla «nombra al organismo como fuente, nunca mandes a su teléfono» se aplicó a las guías **nuevas** desde el 3-sep, y las anteriores conservaron los números del NHS que las traducciones arrastraron. **93 líneas reescritas en 43 guías**, revisadas una a una; el número vive ahora solo donde conoce el país del lector (el aviso de urgencia y el descargo). Candado: `tests/test_guide_safety.py`. |
| **Tres listas de idiomas más, y una etiqueta en español** | Instancias novena a duodécima del mismo patrón. (1) `guides.ts` reconocía el encabezado de preguntas en tres idiomas → **180 guías alemanas, rusas y árabes sin datos estructurados de FAQ**. (2) Cada guía fuera de la raíz declaraba `inLanguage: 'es'` — **240 páginas diciendo a Google que el artículo está en español**. (3) `astro.config.mjs` listaba cuatro locales → las 188 URLs rusas y árabes del sitemap sin ninguna alternativa. (4) El troceador etiquetaba en español el texto anterior al primer título («Introducción») y esa etiqueta viajaba a la cita: **160 citas decían que un documento inglés tiene una sección «Introducción» que no existe**. Ahora no se nombra una sección que el documento no tiene, y las palabras que unen la cita («section», «p.») están en el idioma de la guía — el título y la sección citados siguen en el idioma del documento, que es lo que las hace comprobables. |
| **Gemelas invisibles** | Cuatro temas llevan dos etiquetas (`constipation`/`estrenimiento`, `ear_infection`/`otitis`, `headache_en`/`cefalea`, `vaccines_en`/`vacunas`) porque la guía inglesa se ancló en fuentes inglesas. El selector de idioma comparaba la etiqueta literal, así que **esas guías no ofrecían ninguna traducción, en ninguna dirección**. El generador ya lo sabía (`SAME_SUBJECT`); ahora esa lista se exporta al sitio en vez de duplicarse. |
| **Ruso y árabe (3-sep)** | ✅ Seis idiomas. El criterio que decidió los dos: **si la OMS publica en ese idioma hay corpus abierto garantizado** (sus seis lenguas oficiales van con CC BY-NC-SA, la licencia ya aceptada). 15 fichas de la OMS en ruso y 15 en árabe, cada URL comprobada con un 200 — cuatro que existen en otros idiomas **no existen en ruso** y se descartaron en vez de adivinarlas. Índice: **288 documentos, 6.548 pasajes**; ruso y árabe entran bajo el 10% del corpus y reciben el impulso de idioma escaso automáticamente. Detalle en `FUENTES/RUSO.md` y `FUENTES/ARABE.md`. |
| **Lo específico del ruso** | **Ningún país ruso-hablante estaba en el selector**, así que un padre en Moscú recibía «tu número de emergencias local» en vez de 103 — añadidos RU, KZ, BY y UA con sus números. Y el generador de URLs **tiraba los caracteres no latinos**: las dos primeras guías rusas se llamaron `s.md` y la segunda sobrescribió a la primera, sin error. El slug translitera ahora cirílico y árabe (y el test lo fija). Detección de idioma por **alfabeto**, no por vocabulario: contar marcadores latinos en una frase cirílica es compararla con vocabularios sin una letra en común. |
| **Lo específico del árabe** | Primer idioma **de derecha a izquierda**. Las 43 declaraciones de CSS con dirección física pasaron a propiedades lógicas **antes** de que el árabe existiera, para que fueran 43 cambios comprobables solos. Fuente Noto Sans Arabic cargada **solo donde hace falta** (ninguna de las tres del sitio tiene glifos árabes). Y las fechas tienen configuración propia: `toLocaleDateString('ar-SA')` devuelve fecha **hijri**, así que un artículo de ayer se leería «12/03/1448». |
| **Dos fallos árabes que solo salen probando** | Son de **gramática**, no de vocabulario. (1) Los verbos se conjugan por género: «لا يستجيب» es *él* no responde; una madre escribiendo sobre su hija teclea «لا تستجيب» — todos los patrones estaban en masculino y **la mitad de los niños no disparaban las alarmas**. (2) El **dual**: «شهران» es *dos meses*, no «2 meses», y el analizador de edades lee dígitos — la regla del lactante de menos de 3 meses con fiebre estaba muda justo para la edad por la que existe. 34 patrones nuevos y `tests/test_triage_ar.py`. |
| **⚠️ El fallo grave del check: la API rechazaba idiomas** | `POST /api/ask` validaba `lang` contra un patrón **escrito a mano**, `^(es\|en\|fr)$`. La web manda su idioma explícitamente, así que **desde el despliegue del alemán esta mañana el chat alemán devolvía 422 a cada pregunta** — la página cargaba y no respondía nada. El ruso habría entrado igual. Peor aún: **el test que debía cazarlo afirmaba el bug** («comprueba que el alemán da 422», escrito cuando solo existía el francés), así que al añadir alemán pasó en verde y lo tapó. El patrón sale ahora de `SUPPORTED_LANGS` y el test lee la lista real en vez de una copia. Es la séptima vez que aparece el mismo patrón: una lista de idiomas escrita a mano en un sitio que nadie mira. |
| **Fuentes alemanas (paso 1)** | ✅ Rastreo hecho y documentado en `FUENTES/ALEMAN.md`. **El alemán está peor servido que el francés**: no existe OMS en alemán (`who.int/de` da 404) y no hay equivalente de Canadá. Abierto solo el **RKI** (reproducción no comercial sin cambios y con la fuente) y `gesund.bund.de` **solo como cita suelta**; cerradas la BZgA (`kindergesundheit-info`, el portal para padres, permiso escrito), los pediatras del BVKJ, la AWMF y el portal austríaco. Aplicada la misma opción A del francés: **30 RKI-Ratgeber** añadidos. Índice: 258 documentos, 6.294 pasajes. Se asume y se dice: buena parte del consejo en alemán se apoya en guías británicas, españolas y estadounidenses traducidas al escribir, con el organismo nombrado en cada frase. |
| **Triaje y motor en alemán (paso 2)** | ✅ Las 25 reglas con `reason_de` y **129 patrones alemanes**, edades («2 Monate», «drei Jahren», «6 Wochen»), detector de idioma a cuatro bandas (ä ö ü ß más «das/der/die»), banners, mensajes fijos y las cuatro herramientas deterministas (dosis, suero, vacunas, foto) en alemán. Antes de esto, un padre que preguntaba «Wie viel Nurofen bei 14 kg?» recibía la respuesta **en inglés**, de la herramienta cuyo sentido es poder leerla a las tres de la mañana. |
| **Web y guías en alemán (paso 3)** | ✅ **60 guías alemanas, 0 rechazos** (241 en total: 61 en / 60 es / 60 fr / 60 de). Sitemap: **377 URLs** con `hreflang` a cuatro bandas. Árbol `/de` completo clonado del francés — y esta vez clonar es seguro, porque las palabras ya no viven en las páginas. Las dos últimas plantillas que sí las tenían (ficha de medicamento y calendario por país) se generan ahora para los cuatro idiomas desde una sola, y `dosepages.ts` dejó de llevar un campo de slug por idioma. |
| **Dos mejoras que salieron del francés y valen para todos** | El parser de artículos ya no tira un borrador bueno por olvidar el marcador `BODY:` (y el error muestra qué llegó, en vez de un mensaje mudo); y una regla nueva prohíbe mandar al lector a un servicio de otro país — la primera guía francesa remitía a **«NHS 111»**, que en Francia no existe. Nombrar al organismo como fuente sigue siendo obligatorio; mandar a su teléfono, no. |

### Posicionamiento — reestructuración del 2-sep-2026

**Punto de partida, medido en el registro del servidor** (7 días de Googlebot, 14 de visitas): 1.494 páginas en el sitemap de las que **1.442 eran de dosis** (96 %), Google había rastreado 13 de ellas y habían traído **0 visitas**; todas las entradas desde Google aterrizaban en guías. Es decir: el 96 % del sitio era ruido que se comía el presupuesto de rastreo del 2,5 % que funciona.

| Qué se hizo | Antes | Ahora |
|---|---|---|
| Páginas de dosis | 1.442 (una por marca **y peso**, 216 palabras, casi idénticas) | **42**, una por medicamento con la **tabla completa de 5 a 40 kg**; las viejas redirigen con un solo patrón en Caddy |
| Guías | 30 | **121** (61 EN + 60 ES, 53 emparejadas) |
| Calendarios de vacunas | 1 herramienta | **6 páginas por país** (ES/GB/US en los dos idiomas), enlazadas desde la herramienta |
| Enlaces entre guías | **0** — cada guía era una isla | 3 relacionadas en cada una, empezando por su mismo tema |
| Datos estructurados | MedicalWebPage | **+ FAQPage** con las preguntas que cada guía ya traía |
| Índice de guías | lista de 30 tarjetas | buscador instantáneo (sin librería) sobre 60 |
| Composición del sitemap | 96 % dosis | **66 % guías**, 23 % dosis |

**Coste de las 121 guías: 0,8 USD** del saldo de DeepSeek (queda el 91,9 %).

**Dos fallos propios encontrados y corregidos por el camino**:
1. `pending_topics` se leía **una sola vez** al empezar la tanda, así que el filtro anti-duplicados no veía lo que la propia tanda acababa de escribir: se publicaron las dos caras de varios temas (hives/urticaria, screen_sleep/sueño_pantallas…). Ahora se relee antes de cada artículo, con guarda para que un tema que falla no se reintente en bucle infinito.
2. El criterio para decidir "mismo tema" era el **solapamiento de fuentes**, y se pasa: quería borrar la guía de lactancia por la de alimentación complementaria, y la de asma por la de crisis asmática. Sustituido por una **lista explícita** (`SAME_SUBJECT`, 15 parejas que son la misma palabra en dos idiomas), decidida a mano y anotada. Se retiraron 9 guías duplicadas con 301 a la que se queda.

**El compositor del chat tapaba el pie de página** de forma permanente (`position: fixed`): la página reserva ahora la altura real del compositor, medida en vivo.

### LaunchLeague — medido y cerrado (2-sep-2026)

**Veredicto: la ficha se queda (es gratis), la insignia se retira.** Siete días en su tabla, contados en el registro del servidor y no en su panel:

| Qué | Cuánto |
|---|---|
| Visitantes distintos desde LaunchLeague | **20** |
| Que hicieron **una sola** pregunta al bot | **0** |
| Páginas que miraron | la portada, 14 veces `/dose`, 9 veces `/guides`, una guía |

Miraron el escaparate y se fueron: su público son fundadores, no padres. Comprobado además que **no exigen la insignia** para estar listado (ni en «how it works» ni en «pricing»), así que retirarla no cuesta el puesto.

Lo retirado es la insignia de las dos portadas, por dos motivos: pone una marca de concurso de startups en una web de salud infantil, y suelta un enlace saliente desde las páginas más valiosas que tenemos. El bloque está en git (`2577b0d`) y los dos SVG siguen en `public/`, así que volver a ponerla es un `git revert`.

**De paso, el dato que sí importa**: en esos mismos siete días, **Google ya manda tráfico** a la web y X unas pocas visitas. Ahí es donde está el crecimiento.

## Trabajo del 26-ago-2026 (tarde)

| Qué | Estado |
|---|---|
| **Recuperación cruzada de idioma** | ✅ Golden set **0,964 → 1,00**. Puente español→inglés **mínimo** (11 entradas) solo para lo que no existe en español — sueño, pantallas, actividad física —, comprobado documento a documento; disparadores de varias palabras en los sinónimos ("stomach bug" ≠ "stomach pain"). Medidas y **descartadas**: la tabla es→en completa (0,891) y preferir la lengua de la pregunta (rompe las preguntas en inglés, que necesitan las hojas españolas). Candado en tests para que el puente no se ensanche sin volver a medir. |
| **Licencias** | ✅ Las 7 dudosas, cerradas: 6 en `citar_solo` con motivo anotado, 1 excluida (Elsevier). Y lo importante: `citar_solo` **se hace cumplir** — el generador de artículos filtraba los pasajes de apoyo pero no las anclas, y el tema de atragantamiento colgaba de un libro con ISBN (no se publicó nada desde él). Test sobre los 78 temas. |
| **Años de las hojas SEUP** | ✅ D3 cerrada: las 26 hojas "Información para padres" son de **mayo de 2025** (traumatismo craneal, agosto), la guía de urgencias de **abril de 2020**, según los metadatos de los PDF y la URL pública. Es el año de esta edición, no necesariamente el de la primera redacción; anotado en el catálogo. |
| **Guías publicadas** | ✅ De 4+3 a **16 en inglés y 13 en español**, en línea y en el sitemap (1.487 URLs). Sin difundir en redes para no hacer spam. |
| **Verificador de artículos** | ✅ Rechaza el artículo escrito en un idioma distinto al pedido (salió uno en español dentro de `/en`) y los temas `_en` ya no se ofrecen en español (generaban una guía gemela). |
| **Guard de dosis** | ✅ Estrechado: los mg siempre cuentan como dosis, los ml solo se perdonan cuando el texto habla de suero, agua, leche o tomas, y si no está claro cuenta como dosis. Antes bloqueaba las guías de vómitos y gastroenteritis por los volúmenes de suero oral del SEUP. |
| **OCR de las 50 consultas** | ✅ Hecho (402 páginas, 179.034 palabras, OCR limpio) y **medido**: el libro es de **2008**, para profesionales, nunca es la 1.ª fuente y no llega ni a las preguntas de sus capítulos → **excluido del índice**, OCR guardado y ficha conservada para revertir (L27). Identificado de paso: ISBN 978-84-691-1444-5, **no es de la AEPap** sino de cuatro pediatras con patrocinio de Merck. |
| **Dosis: fuente autorizada** | ✅ La excepción que permite dar una cifra ya no es "parece una tabla de dosis" (cualquier manual profesional lo parecía, con corticoides y antibióticos dentro) sino `dose_source: true` en el catálogo — hoy solo la guía de dosificación de la AEPap, la misma que usa la calculadora. |
| **Palabras con doble sentido** | ✅ «pis», «oído» y «pecho» citaban la hoja equivocada (deshidratación, otitis, lactancia) en preguntas que el corpus no cubre; ahora son frases. Un umbral de relevancia se midió y se **descartó**: los rangos se solapan con casos legítimos (L25). |
| **Aclaración** | ✅ Leía solo la pregunta cruda y mandaba al menú preguntas claras («se ha desmayado en el colegio», «llora sin parar»): 4 de 55 del golden set. Ahora lee la pregunta expandida con sinónimos, igual que la búsqueda. Redactadas 50 → **54 de 55**. |
| **Watchdog** | ✅ Vigila los cuatro servicios cada 10 min (antes un `pedibot-telegram` muerto era silencioso). |
| **robots.txt** | ✅ Estaba dando 404 y **Googlebot lo pidió 6 veces** en 24 h. Añadido con la línea `Sitemap:` y dejando fuera `/api/`, `/a/` y `/admin`. **Sitemap ya leído con 200 el 27-ago a las 07:05:43 UTC** (verificado en el registro de Caddy; el panel de Search Console va con retraso). |
| **Imagen social** | ✅ `og:image` apuntaba a `/og.png`, que **no existía**: todo enlace compartido salía sin miniatura. Generada (1200×630, marca del logo) con `scripts/make_og_image.py`, junto con el `favicon.ico`. |
| **hreflang** | ✅ Declaraba la gemela cambiando solo el prefijo de idioma → `/es/guides/<slug-inglés>`, un 404 (de ahí los 404 de Google en el registro). Ahora cada guía busca su gemela por `topic` y omite la etiqueta si no existe. **Las 15 guías EN y las 15 ES quedan emparejadas 1 a 1.** |
| **Enlaces de redes** | ✅ Los textos de las guías inglesas enlazaban `/en/guides/…`, que no existe (el inglés vive en la raíz). |
| **URLs sin redirección** | ✅ Astro construye con `trailingSlash: 'never'` pero Caddy redirigía cada URL a la versión con barra: las 1.488 del sitemap respondían 308 antes de servir. Corregido; ambas formas dan 200 directo. |
| **LaunchLeague (27-ago)** | ✅ Alta hecha por el operador; insignia puesta en el bloque «Dónde está PediBot» de las dos portadas (no en las 1.488 páginas, para no soltar un enlace saliente en todo el sitio). Los dos SVG son **suyos pero servidos desde nuestro dominio**: sin petición a terceros, que es lo que promete `/legal`, con cambio automático claro/oscuro. ⚠️ **La ficha todavía no aparece en su portada** (0 menciones de PediBot en el HTML, 14 productos listados y ninguno es el nuestro): o está pendiente de revisión o entra en la jornada del lunes. Valor esperado: enlace do-follow mientras dure la jornada; su público son fundadores, no padres. |
| **Duplicado de golpe de calor** | ✅ Dos claves de tema (`heat` y `golpe_calor`) generaron dos guías inglesas del mismo asunto. Retirada una con **301** hacia la que se queda, y regla nueva para que no se repita (los `compare_*` quedan exentos). |

## Lo que NO está hecho / conocido

- **Recuperación de tablas de dosis floja**: la guía AEPap es una tabla y BM25 no la puntúa bien ("how much paracetamol for 12 kg" no sube la tabla pediátrica). Mitigado por el enrutador determinista (pregunta con peso → calculadora sin LLM). Solución de fondo: embeddings (extra `[embeddings]`, e5-small) en F2.
- **El golden set mide triaje/recuperación/enrutado, NO la calidad de la redacción**: fidelidad a la fuente y validez de citas con el LLM real quedan pendientes de la clave de DeepSeek (métricas `citation_validity` y juez de fidelidad del PRD §5.5).
- Fallos abiertos del golden set (2 de 60): g49 "¿cuánto tiene que dormir un niño de 2 años?" (la guía OMS está en inglés y no hay expansión es→en, I-17) y g60 recién nacido que rechaza tomas (fuente AEP no sube; el triaje sí lo marca urgente).
- **OCR pendiente** de `las_50_principales_consultas.pdf` (sin tesseract local).
- **Fuentes en inglés** se indexan tal cual; la expansión de sinónimos solo va en→es (para una pregunta en español sobre una guía de la OMS en inglés no hay expansión es→en).
- **Producción**: el servidor es de 1 vCPU/2 GB (no CX22): suficiente para API+bot; la web se construye en el PC. Cloudflare en modo DNS-only; si se activa el proxy naranja hay que poner SSL «Full (strict)». De las ideas aceptadas faltan: T-06 Virtuals ACP (investigado; tras F4), W-06 pediatras revisores (contactos). Telegram: falta el token del bot nuevo. Hechas el 25-ago: diario+recordatorio, checklist urgencias, explícaselo a mi hijo, voz, mapa, temporada, compartir, 685 páginas de dosis SEO por idioma, /support, /legal.
- 7 fuentes con licencia `?` en el catálogo (aceptadas provisionalmente como `citar_solo`).

## Deudas técnicas

| Id | Qué | Prioridad |
|---|---|---|
| ~~D1~~ | ~~Rotar todas las claves de `pass.txt`~~ — **HECHO por el operador el 26-ago-2026**. Era la única deuda de seguridad viva. | ✅ cerrada |
| D2 | OCR de `las_50_principales_consultas.pdf` (VPS/WSL). | F1 tail |
| D3 | Confirmar año de las hojas SEUP. | F1 tail |
| D4 | Instalar Node/Astro para la web real. | F3 |
| D5 | ~~Dominio~~ pedibot.xyz comprado; DNS Cloudflare → apuntar al VPS en F4. | F4 |
| D6 | Sin remoto git: 234 commits en un solo disco, sin copia del historial fuera de él. | alta |
| ~~D7~~ | ~~El juez de fidelidad nunca se ha ejecutado~~ — **ejecutado el 8-sep-2026**: 99 juzgadas, **fidelidad 0,788**, `citation_validity` 0,99, coste 0,06 $. Ya hay línea base. | ✅ medida |
| D10 | **Fidelidad 0,788, y 17 de los 21 fallos son alarmas.** Intento medido y descartado el 8-sep (`answer_v5`: 0,747, ver L61): dar permiso para no citar afloja el hábito en todas las demás frases. Hay que sacar la orden de urgencia del cuerpo o darle fuente que la contenga — decisión de producto. Las reglas 2/3 del prompt (cita y organismo obligatorios, con el verificador tirando la respuesta) contradicen la 10 (sin cita si nada lo respalda), y el modelo resuelve inventando la atribución de NUESTRA orden de urgencia. Decisión pendiente: ver L59. | **la más alta** |
| ~~D8~~ | ~~Producción sin desplegar~~ — **desplegado el 8-sep-2026** y verificado contra el sitio vivo. | ✅ cerrada |
| D9 | Las etiquetas de forma de las MARCAS siguen como las escribe el fabricante (decidido: no se traducen); las genéricas ya van en los ocho idiomas. | cerrada por decisión |

## Próximos pasos (orden propuesto)

Los cuatro que había aquí están **hechos** desde hace semanas (clave de DeepSeek, API con SSE y
persistencia, embeddings, Astro con widget). Se sustituyen por lo que la revisión del 8-sep dejó
abierto, en orden de consecuencia:

1. **Desplegar.** Los arreglos del 8-sep (triaje del lactante, calculadora, página compartida)
   están en el PC y **no en el VPS**: `make web-build && bash ops/deploy.sh 46.225.74.13`.
2. **Un remoto privado.** 234 commits viven en un solo disco y no hay copia del historial fuera
   de él (D-02 sigue sin decidir desde el 24-ago). Es lo único irreversible de esta lista.
3. **Decidir de quién es la orden de urgencia** (D10 / L59). Es el número que mide la promesa
   central del producto —«cada afirmación lleva referencia»— y está en 0,788. Re-medir cuesta
   0,06 $ y unos 35 min.
4. **Diez padres.** De las 128 respuestas de la base, 121 son `test` del operador: web 1,
   Telegram 2, agente 2. El cuello de botella no es el código.
5. Las tres guías que solo existen en inglés (asma, ibuprofeno, paracetamol): o se traducen a los
   siete idiomas o se retiran, porque en ellas el selector de idioma no ofrece nada.

## Calendario

- 2026-08-24 — Arranque v2: docs, catálogo, ingesta completa, triaje, calculadora, motor con verificador, CLI, 80 tests, boceto web v1.
- 2026-09-08 — **Revisión completa y depuración.** Seis fallos arreglados, tres de ellos de seguridad clínica: los 28 patrones de la regla del lactante con fiebre no se evaluaban nunca (cinco de ocho idiomas se quedaban en rutina al decir «lactante», «Säugling», «младенец», «رضيع»); la calculadora daba la dosis completa de ibuprofeno a un bebé de dos meses con el aviso debajo; los avisos salían como identificadores internos del código; la lista de botes salía en castellano en los ocho idiomas; `/api/photo` no pasaba el idioma y devolvía la frase de emergencia en inglés; la página de respuesta compartida estaba en dos idiomas y sin dirección de escritura para el árabe. Tres candados nuevos (`test_infant_words.py`, `test_dose_refer.py`, y la página compartida en `test_i18n_parity.py`). 1.302 → 1.357 tests. Golden set intacto: 111/111 en las seis métricas.
- 2026-09-08 (tarde) — **Segunda pasada.** Telegram perdía el idioma elegido en cada reinicio; `/stop` y `/country` contestaban en dos idiomas. Y el hallazgo de fondo: el evaluador construía un motor sin tres de los cinco enrutadores, y el conjunto dorado no tenía ni un caso de vacunas, así que `routing: 1.0` medía diez casos de 111 — al arreglarlo saltó que «in the UK» y «in the US» no se leían como país. 118 casos, 1.396 tests. Primera medición del juez de fidelidad: **0,788** (D10).
- 2026-09-08 (noche) — **Desplegado y verificado en producción.** Después del despliegue, la misma pregunta en los ocho idiomas contra el sitio vivo destapó que el alemán no lee su propia forma de decir la edad; el barrido de 6 situaciones × 8 idiomas destapó tres más, y en el peor sitio: costarle respirar no era emergencia en alemán, portugués ni hindi. `answer_v5` escrito, medido y **descartado** (0,788 → 0,747, L61). 1.453 tests.
- 2026-09-08 (cierre) — **Barrido de 12 situaciones × 8 idiomas: veinte agujeros.** Todos con la regla escrita y la red más estrecha en alguna lengua. El peor: el hindi conjuga por género y solo estaba el masculino, así que una hija que dice que quiere morir no disparaba la regla del 024. Más: la anafilaxia sin la forma verbal en cinco lenguas, la lejía nombrada solo en tres, «doce horas sin mojar el pañal» en ninguna, y los guiones sin normalizar en las reglas («my 5-day-old» ≠ «my 5 days old»). Regla nueva `meningitis_signs` (urgente, fuente MedlinePlus): la de la cefalea implementaba dos de los cinco signos de su propia hoja. 33 reglas, 1.550 tests, dorado 1.0 en las seis métricas.
- 2026-09-08 (2.ª oleada) — **Otras 8 situaciones × 8 idiomas: 19 agujeros y un falso positivo.** El falso positivo es el que más enseña: la prueba del vaso con resultado BUENO («a rash that fades when I press it») daba emergencia. Dos formas nuevas de la red estrecha: el orden de la frase (`blood_in_stool` casaba «heces…sangre» y no «sangre…en las heces» en cinco lenguas) y el participio («ha perdido el conocimiento» frente a «perdió», en castellano). 1.679 tests, dorado 1.0, desplegado y verificado vivo.
